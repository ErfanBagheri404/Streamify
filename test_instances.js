// Test script to check if dynamic instances are loaded
import { DYNAMIC_INVIDIOUS_INSTANCES } from './components/core/api';

console.log('Dynamic instances:', DYNAMIC_INVIDIOUS_INSTANCES);
console.log('Number of instances:', DYNAMIC_INVIDIOUS_INSTANCES.length);